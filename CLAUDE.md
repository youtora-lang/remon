# remon 開発ガイド（Claude Code 用）

このリポジトリは、中学1年生向けの5教科テスト対策学習アプリ「remon」です。
教科書準拠の4択クイズを、間隔反復で出題する PWA です。ビルド工程はありません。

## ファイル構成

- `index.html` … 本体。全ロジックと問題データ（PRELOAD 配列）を内包
- `sw.js` … Service Worker（キャッシュ管理）
- `validate-questions.js` … 問題データの検証スクリプト（Node、依存なし）
- `manifest.json` / `manifest-3.json` / `icon-192.png` / `icon-512.png`
- `README.md` … リポジトリの説明（人が最初に読む1枚）
- `CLAUDE.md` … このファイル

問題データはすべて `index.html` の `PRELOAD` の1行に埋め込まれています。
この1行だけで約244KB、ファイル全体の約76%です。整形して改行を入れないでください。

## 問題データの形式

```json
{"question":"...","choices":["正解","誤答","誤答","誤答"],"explanation":"..."}
```

- `choices[0]` が正解。**正解フラグは存在せず、位置だけが正解の印です。**
  ここを間違えると誤答が正解として扱われ、誰も気づけません
- `choices` はちょうど4個
- 1単元あたり5〜8問
- 空の単元は `"questions":[]`

## 対応教科書（準拠先）

| 教科 | 出版社 | 教科書名 |
|---|---|---|
| 英語 | 東京書籍 | NEW HORIZON English Course 1 |
| 数学 | 啓林館 | 未来へひろがる数学 1 |
| 国語 | 光村図書 | 国語 1 |
| 理科 | 啓林館 | 未来へひろがるサイエンス 1 |
| 地理 | 帝国書院 | 中学生の地理 世界の姿と日本の国土 |
| 歴史 | 育鵬社 | 新しい日本の歴史 |

## 「新しい問題を追加してください」と言われたときの標準作業

「新しい問題を追加して」「続きから2単元ずつ追加」など、追加先を指定しない
依頼はすべてこの標準作業を指します。

1. `PRELOAD` を調べ、各教科（国語・数学・理科・地理・歴史・英語）について、
   配列の並び順（＝教科書の目次順）で最初に見つかる空の単元を2つ選ぶ
   （空単元が1つならその1つ、0なら飛ばす）
2. **空き枠が尽きた教科は飛ばし、残っている教科から補って1バッチ12単元を保つ。**
   教科ごとの空き枠数は不均一なので、後半ほど特定の教科に寄る
3. **選んだ単元の一覧を提示してから作業に入る**
4. 各単元を5〜8問ずつ埋める。1単元6問を基準にする

**全教科の空き枠が尽きたあとは、既存単元に問題を足します。**
1単元6〜7問のところを10問程度まで増やす。追加は末尾に足し、
既存の問題文は書き換えない。

本文が必要で飛ばした単元は、**準備中のまま残します。**
教科書の本文を渡されたときに作る枠として扱います。

## どの単元に問題を作るか

**教科書の年間指導計画で配当時間が割り当てられていない教材には、
問題を作りません。** 授業で扱わない以上、定期テストの範囲にならないためです。

ただし、時数のない枠は `PRELOAD` から落としてあり、新しい単元枠も作らないので、
**追加のたびに時数を調べ直す必要はありません。**
`PRELOAD` に枠があるものは、すべて作ってよい単元です。
（中1国語では「季節のしおり」の4枠が時数なしにあたり、削除済みです）

教科書の本文を見ないと作れない単元は、次のように扱います。

- **英語の Unit 系**：単元名の括弧に文法項目が書かれていれば
  （例：`Unit 4 … P3（命令文 Come/Be/Don't）`）、本文がなくても
  その文法項目に沿って作ってよい。本文の細部は問わない。
  括弧がなく本文の内容にしか手がかりがない単元は飛ばす
- **国語の教材文系**：本文にしか書かれていないことは問わない。
  本文の外でも確かめられること（ジャンル、読み方、作者、題材、
  広く知られた枠組み）だけを問う。それでも作れないものは飛ばす

飛ばすときは **「この単元は教科書の本文が必要」と報告し、
その教科の次の空枠を代わりに選びます。**

文法系（Grammar、文法への扉）や、数学・理科・地理・歴史の単元名は
内容が一意に決まるので作って構いません。

## 問題作成の共通ルール

1. 問題文は日本語の指示文または場面設定から始める
2. 誤答は「もっともらしい誤り」にする。無関係な語や明らかに変な選択肢は避ける
   （消去法で解けると学習効果が下がるため）
3. 選択肢は同じカテゴリ・同程度の長さでそろえ、形式から正解が推測されないようにする
4. 解説は1〜2文。「なぜ正解か」に加え、誤答が典型的な勘違いならその理由にも触れる
5. **同一教科内のすべての既存問題文と重複させない。**
   `syncPreload()` は学習履歴を「教科キー＋問題文」で張り直すので、
   同じ問題文が2つあると履歴が片方に寄り、もう一方は未学習のままになります。
   さらに `syncPreload()` は問題文が同じものを1つしか取り込まないため、
   その単元の問題数も静かに減ります。
   以前この原因で9件（国語4・英語5）が消えていました（解消済み）
6. 教科書に載っている用語・定義・表記を優先し、独自の言い換えをしない
7. 数式・座標に使う記号は半角にする（`=` `-` `+` `(` `)`）。
   全角を使うのは「面積＝底辺×高さ÷2」のように、日本語の文章として
   書く場合だけにする
8. `=` のまわりに空白を入れるかは、**追加する単元の中で統一することを優先する。**
   新しく作る場合は空白なし（`y=3x`）を既定とする。
   数学135問の全体では、問題文20対13・選択肢76対17・解説128対31で、
   いずれも空白なしが多数派
9. 英語の文法解説で語の並びを示す `＋` は全角、前後に空白を入れない
   （例：`主語 I＋動詞 have＋目的語 a dog の順。`）

## 教科ごとの重点

- **英語**：日本語文→正しい英訳を選ぶ4択。時制・語形・語順・冠詞など中学生が
  つまずく要素を誤答に仕込む。ただし日本語文で正解が一意に決まるようにする
- **数学**：計算過程を要する問題中心。誤答に「符号ミス」「計算順序の誤り」
  「公式の一部忘れ」など典型的ミスを反映し、解説で正しい途中式を示す
- **国語**：文学・詩教材で本文が手元にない場合は、テキスト固有の細部を問わず
  一般的な文学概念で対応する。文法・漢字は定義と具体例をセットで問う
- **理科**：観察・実験の手順や安全上の注意も含める。用語の暗記だけでなく
  「なぜそうなるか」を問う設問を混ぜる
- **地理・歴史**：年代・地名・人名は教科書の表記に厳密に合わせる。歴史は因果関係、
  地理は特色とその理由をセットで問う。
  教科キーは `chiri` / `rekishi`（旧 `shakai` は `splitShakai()` が移行する）

## 正確性の担保（最重要）

- 教科書本文やページが手元にない場合は、一般的に確立した教育内容の範囲にとどめ、
  細部の断定を避ける
- 不確かな固有名詞・年代・数値は使わない。曖昧なら問題自体を作らない

## 検証スクリプト

リポジトリ直下で実行します（Node のみ、インストール不要）。

```bash
node validate-questions.js
```

検査するのは次の7項目です。違反は件数と該当箇所（教科・単元・単元内の問題番号）
つきで表示されます。

- `PRELOAD` が JSON として正しく読めるか
- 各問題に `question` / `choices` / `explanation` が揃っているか
- `choices` がちょうど4個か
- 同一問題内で選択肢が重複していないか
- 同一教科内で問題文が重複していないか
- 単元名が重複していないか
- 想定外の文字が混じっていないか（ひらがな・カタカナ・漢字・英数字・
  一般的な記号・ギリシャ文字だけを許す。キリル文字や半角カタカナ、
  絵文字などの混入を見つける）

このほかに**警告**として、数式・座標に使う記号の全角（`＝` `－` `＋` `（` `）`）を
報告します。「面積＝底辺×高さ÷2」のような日本語の文章としての使用は許すため、
式や座標として使われている疑いのあるものだけを拾い、**違反には数えません**。
直すかどうかは中身を見て判断してください。

終了コードは 0=全項目パス / 1=違反あり / 2=読み込み失敗です。警告は終了コードに
影響しません。

既知の未修正分はありません。作業後は**全項目パス（終了コード 0）**であることを
基準にしてください。重複が報告された場合は、今回追加した問題が既存の問題文と
重複しています。

## 作業後に必ず行うこと

1. **`node validate-questions.js` を走らせ、全項目パス（終了コード 0）を確認する**
2. `index.html` フッターの `Updated ...` を現在の日本時間（UTC+9）に更新する
3. `sw.js` のキャッシュ名（`remon-vNN`）を1つ上げる。
   **上げないと Service Worker の更新が発火せず、端末に反映されません**
4. 次のいずれかを伴う変更をしたときは、`README.md` も同じコミットで直す。
   - 機能を追加・削除した
   - ファイルが増えた・減った
   - データ構造や保存キーを変えた
   - 検証・デプロイの手順を変えた

   **問題を追加しただけのときは `README.md` を触らない。**
   問題数や単元の埋まり具合は、古くなるので README には書かない方針です
   （最新の内訳は `node validate-questions.js` の出力で確認します）
5. どの教科のどの単元を埋めたか、単元数・問題数とあわせて報告する

## PRELOAD_STRUCT_VERSION の扱い

**上げる必要はありません。**
`syncPreload()` が起動のたびに `PRELOAD` で `state.questions` を作り直すので、
問題の追加・修正・削除も、単元枠の追加・削除・名称変更も、版に関係なく反映されます。

値と `state.preloadStructVersion` への代入は、バックアップの互換のために
残してあるだけです。分岐には使っていません。

## 学習履歴の仕組み（壊さないこと）

学習履歴は `state.history[教科キー][位置]` に保存されます。
**キーは問題文ではなく、`state.questions[教科キー]` の中での位置（添字）です。**

`syncPreload()` は作り直しの前後で「教科キー＋問題文」を仲立ちにして
履歴を新しい位置へ張り直します（`histByText`）。この張り直しがあるので、
問題の追加・並べ替え・単元枠の変更があっても履歴は残ります。
逆に、`questions` を作り直して履歴を張り直さないと、
全問の履歴が別の問題にずれて付きます。

ただし **問題文を編集すると、その問題の学習履歴は失われます。**
張り直しのときに新旧が別の問題として扱われるためです。誤字の修正であっても、
その問題の正誤記録と復習タイミングは初期状態に戻ります。

## やってはいけないこと

- 既存の問題文を編集する（学習履歴が失われるため）
- 必要のない `PRELOAD_STRUCT_VERSION` の更新
- 既存の問題を並べ替える、削除する
- `PRELOAD` の1行を整形して改行を入れる

## コミットと push

- コミットは `youtora-lang` 名義で行う
- **コミットメッセージに `Co-Authored-By` 行は付けない**
- 内容を確認できたらコミットし、指示があれば `git push origin main` する
- push すると GitHub Pages のデプロイが走り、数分後にアプリへ反映される
